{
  "name": "redux-counter",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-redux": "^7.1.0",
    "react-scripts": "0.9.x",
    "redux": "^4.0.3"
  },
  "devDependencies": {},
  "scripts": {
    "preinstall": "npm i -g json-server",
    "server": "json-server --watch db.json --port 3001",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
